apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "firefly-erc1155.fullname" . }}-test-connection"
  labels:
    {{- include "firefly-erc1155.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "firefly-erc1155.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

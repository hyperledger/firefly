apiVersion: v1
kind: Service
metadata:
  name: {{ include "firefly-erc1155.fullname" . }}
  labels:
    {{- include "firefly-erc1155.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firefly-erc1155.selectorLabels" . | nindent 4 }}
